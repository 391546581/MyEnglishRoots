apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rootflow-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: rootflow.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: rootflow-service
            port:
              number: 80
